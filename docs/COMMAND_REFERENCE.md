# COMMAND REFERENCE

## UX
`/start`, `/quickstart`, `/mode`, `/status`, `/suggest`, `/workspace`,
`/examples`, `/search`, `/explain`, `/auto-run`, `/capability-map`

## Build
`/new-engine`, `/start-engine`, `/preview-engine`, `/finalize-engine`,
`/export-engine`

## Diagnostics
`/engine-diagnostics`, `/schema-status`, `/schema-drift`, `/readiness`, `/health`

## Performance
`/evaluate-engine-performance`, `/token-footprint`, `/context-endurance`,
`/performance-trend`, `/final-monolith-review`, `/monolith-split-recommendation`

## Personality
`/build-agent-profile`, `/agent-status`, `/agent-feedback`,
`/evolve-agent-profile`, `/personality-diagnostics`

## Optimization
`/run-bakeoff`, `/feature-matrix`, `/conflict-matrix`,
`/assemble-optimized-engine`
